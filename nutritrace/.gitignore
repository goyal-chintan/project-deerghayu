# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Vite
.vite/
vite.config.js.timestamp-*

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Local backups (not needed in repo)
.backup-polish/
.worktrees/

# Bind mount data (DB + uploads live on the host, not in git)
data/
# ...but the curated seed datasets (numeric facts + per-row provenance) are
# reviewable source-of-truth and MUST be committed. Raw upstream downloads
# under scripts/data/ stay ignored.
!server/seed/data/
!server/seed/data/**

# Editor
.vscode/
.idea/
*.swp
*.swo


# Local SQLite runtime artifacts
*.db
*.db-shm
*.db-wal

# Debug/test artifacts
.playwright-cli/
output/
